<script src="/socket.io/socket.io.js"></script>
<script>
  let username='<%= user.username %>';
  let usercolor='<%= user.usermeta.nickNameCol %>';
  let usermode='<%= user.usermeta.mode %>';
  let usergroup='<%= group %>';
  let showMenu;
  if("<%= user.usermeta.menuIsOpen %>" === "true"){
    showMenu=true;
  }else{
    showMenu=false;
  }
</script>
<script src="/globals.js"></script>
<script src="/functions.js"></script>